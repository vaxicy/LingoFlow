# 2026-07-16

- 使用 PIL 生成了 LingoFlow 英文版商店截图 5 张（1280×800），输出到 `store-assets/en/screenshots/`。
- 使用 PIL 生成了中英双语宣传图块：`store-assets/global/small-promo-440x280.png` 和 `store-assets/global/top-promo-1400x560.png`，覆盖掉原来的中文-only 版本。
- 新增生成脚本：`scripts/generate-store-screenshots.py` 和 `scripts/generate-store-promo.py`。
- 按用户反馈修复了宣传图块布局：
  - `top-promo-1400x560.png` 右侧翻译卡片的中文翻译被底部截断，通过压缩卡片内行距解决。
  - `02-selection-translation.png` 的工具栏按钮文字溢出和垂直不对齐，通过按文字宽度动态计算按钮宽度并改用 `anchor="mm"` 垂直居中解决。
  - 顺手把 `05-privacy-support.png` 的 CTA 按钮文字也改为垂直居中。
