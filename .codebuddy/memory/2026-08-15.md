# 2026-08-15

## 新增自定义 OpenAI 兼容翻译引擎
- 用户要求翻译引擎支持 OpenAI 兼容接口（任意 /v1/chat/completions 端点均可）。
- 新增 `custom` 引擎，复用现有 OpenAI 兼容调用模式（参考 SiliconFlow/DeepSeek）。
- 改动文件：`js/background.js`(默认值/迁移块/分发路由/translateWithCustom+translateBatchWithCustom)、`popup.html`(引擎 select+menu option+3 个设置行)、`popup.js`(toggleCustomRows/联动/取值/默认值/隐藏输入预填)、`_locales/en`、`_locales/zh_CN`(7 个键)、`README.md` 引擎表。
- 用户明确「先不改版本号」，故 manifest version 保持 1.0.2 未动（commit 7416e33）。
- 自定义端点无 API Key 或响应异常时回退 Google 翻译（单条 18s 超时、批量 30s 超时）。
- Chrome 扩展项目：未 push（用户未要求）。
